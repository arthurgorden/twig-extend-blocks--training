<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      Bonjour
    </title>
    <link rel="icon" href="/assets/images/favicon.png" />
    <link rel="stylesheet" href="/assets/css/styles.css" />
  </head>

  <body>
    <h1>
      Welcome !
    </h1>

    <ul><li>
      {% for product in products|filter((product, key) => key <= 2) %}
        {{ product }}
      {% endfor %}
    </li>
    <li>
      {% for product in products|filter((product, key) => key >= 3) %}
        {{ product }}
      {% endfor %}
    </li>
    </ul>

    <pre>
      {{ dump(products) }}
    </pre>

  </body>
</html>
